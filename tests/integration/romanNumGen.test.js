const FILE_PATH = "../../src/public/scripts/romanNumGen.js";
const { getRandomInt, genRomanNumeral, genInversions, clearInversions, romanToWords, chordsToWords, displayAlphabetProgressionAndWords, NUM_CHORDS } = require(FILE_PATH);

// empty for now
describe("placeholder", () => {
    test("placeholder test", () => {
        expect(true).toBe(true);
    });
});